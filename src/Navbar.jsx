import Button from './Button';

const Navbar = () => {
	return (
		<>
			<div>search</div>
			<div>
				<Button value="Contact Sales" />
				<Button value="Sign in" />
				<Button value="Sign Up for Free" />
			</div>
		</>
	);
};
export default Navbar;
